---
name: transport-booking
description: 当用户需要跨城出行、机票/火车/巴士方案搜索、选择跨城交通方案或创建跨城交通订单时，使用本技能。
---

## 适用范围

处理跨城出行流程，包括明确出发地、目的地、出行日期，搜索飞机/火车/巴士等交通方案，用户选择方案后创建交通订单并引导支付。

## 能力边界

- 只处理跨城交通相关需求。
- 不处理酒店搜索、酒店房型选择或酒店订单创建。
- 不处理同城点到点打车；同城接驳交给 `taxi-booking`。
- 不向用户暴露交通方案底层 ID、订单底层 ID、工具参数或内部判断过程。
- 不能编造航班、车次、班次、票价、余票、时刻或订单状态。

## 基础流程

1. 用户提出跨城出行需求后，先判断是否已经具备交通搜索所需信息。
2. 补齐出发地、目的地和出发日期；如果用户有返回日期、出发时间段、交通方式、预算等偏好，一并用于搜索。
3. 调用 `rideclaw_aggregated_transport_search_v2` 同时检查飞机、火车、巴士等方案。
4. 用户从工具结果中选择具体交通方案后，调用对应的 `create_order` 创建交通订单。
5. `create_order` 成功后，前端基于订单信息创建支付卡片。

## 信息要求

跨城交通搜索通常需要：

- 出发城市或出发站点。
- 到达城市或到达站点。
- 出发日期。
- 单程、往返或多段；用户没说时按单程处理。
- 交通方式偏好、时间偏好、预算、舱位/座席等；没有则不强行追问。

如果目的地是酒店、景点或具体地址，先识别其所在城市；跨城段只搜索城市或站点之间的交通。

## 方案选择

- 调用 `rideclaw_aggregated_transport_search_v2` 后，工具结果对用户可见时，不重复罗列全部方案，只提示用户选择合适的交通方案。
- 用户明确选择某个航班、车次、班次、时间或价格方案时，优先理解为确认创建交通订单。
- 用户反馈价格高、时间不合适、想换交通方式时，按修改需求重新搜索或筛选，不直接下单。

## 确认下单

- 调用 `create_order` 前，必须已经有明确的交通方案。
- 用户选择方案时同时复述出发地、目的地或日期，且与当前上下文一致时，视为确认订单信息。
- `create_order` 成功后，不重复订单明细，只引导用户完成支付。

## 图示流程

1. 用户说明跨城出发地、目的地和出发日期。
2. 调用 `rideclaw_aggregated_transport_search_v2` 同时检查飞机、火车、巴士等方案。
3. 用户从工具结果中选择合适的交通方案。
4. 调用对应的 `create_order`。
5. 前端基于订单信息创建支付卡片。

## 边界指引

- 同城点到点出行交给 `taxi-booking`，不要调用跨城交通搜索。
- 需要酒店或到达后接驳打车时，交给 `travel-planning` 统一拆解。
- 如果跨城目的地是具体酒店、景点或地址，先识别城市级目的地，跨城段只处理城市/站点之间的交通。

## 输出要求

- 工具结果已经展示给用户时，不重复罗列全部方案。
- 信息缺失时，只追问当前最关键的一个字段。
- 不能编造班次、航班、票价、余票或订单状态。
- 调用 `rideclaw_aggregated_transport_search_v2` 后，只提示用户选择合适的交通方案。
- 调用 `create_order` 成功后，只提示用户完成支付。

## 相关工具

- rideclaw_aggregated_transport_search_v2
- create_order
